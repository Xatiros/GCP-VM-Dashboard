# nginx.conf (para el frontend, en la raíz del repo)
events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    server {
        # --- ¡CAMBIO CRÍTICO AQUÍ! Nginx escuchará en el puerto de la variable de entorno ---
        # Asegúrate de que Nginx escucha en el puerto que Cloud Run proporciona (PORT=8080).
        listen ${PORT};
        # Elimina 'default_server' para mayor claridad y compatibilidad
        # --- FIN CAMBIO ---

        server_name localhost;

        root /usr/share/nginx/html;
        index index.html index.htm;

        location / {
            try_files $uri $uri/ /index.html;
        }

        error_page 500 502 503 504 /50x.html;
        location = /50x.html {
            root /usr/share/nginx/html;
        }
    }
}